# Security - Generate with:
# openssl rand -base64 32  (for JWT_SECRET)
# openssl rand -hex 32     (for ENCRYPTION_KEY)
JWT_SECRET=your_jwt_secret_here_generate_with_openssl_rand_base64_32
ENCRYPTION_KEY=your_encryption_key_here_generate_with_openssl_rand_hex_32

# Email Configuration
# Use Gmail App Password (not regular password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_USER=ben@zyroi.com
SMTP_PASS=gxzo zbzs eipb reub

# Application
BASE_URL=https://sss.zyroi.com
ADMIN_EMAIL=ben@zyroi.com
NODE_ENV=production

# Database
DB_PATH=./data/secretsanta.db

# Optional: Send signup emails on server start
SEND_INITIAL_EMAILS=false
